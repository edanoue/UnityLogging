#nullable enable

namespace Edanoue.Logging
{
    /// <summary>
    /// 
    /// </summary>
    public enum LogLevel : int
    {
        NotSet = 0,
        Debug = 10,
        Info = 20,
        Warning = 30,
        Error = 40,
        Critical = 50,
    }
}